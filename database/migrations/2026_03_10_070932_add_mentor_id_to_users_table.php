<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
   public function up()
{
    Schema::table('users', function (Blueprint $table) {
        // This adds the missing link between students and mentors
        $table->unsignedBigInteger('mentor_id')->nullable()->after('admin_role_id');

        // Optional: Add a foreign key to ensure data integrity
        $table->foreign('mentor_id')->references('id')->on('users')->onDelete('set null');
    });
}

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            //
        });
    }
};
